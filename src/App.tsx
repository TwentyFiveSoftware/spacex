import React from 'react';
import styles from './styles/App.module.scss';

const App = () => {
    return (
        <div>
            <h1 className={styles.title}>SpaceX</h1>
        </div>
    );
};

export default App;
